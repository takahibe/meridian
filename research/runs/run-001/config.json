{
  "run_id": "run-001",
  "mode": "live",
  "hypothesis": "Tiny-wallet live shadow of main meridian's strategy. Verify behavior under real fee accrual, OOR dynamics, and close conditions that DRY_RUN cannot simulate. No strategy divergence from main yet — this run validates that the isolated profile produces a reasonable baseline before any A/B experiment.",
  "wallet_seed_sol": "~1-2",
  "max_open_positions": 1,
  "deploy_amount_sol": 0.2,
  "max_deploy_amount_sol": 1,
  "position_size_pct": 0.25,
  "min_sol_to_open": 0.4,
  "notes": [
    "Caps live in profiles/autoresearch/user-config.json and are enforced by the standard deploy_position safety checks, not by autoresearch-specific guards. Upstream's autoresearch capital-budget / daily-loss enforcement was deferred in Phase 1 due to Band-B conflict.",
    "hiveMindPublishMode='off' for this profile — research lessons never flow to shared HiveMind.",
    "Telegram alerts arrive on the shared chat with [AUTORES] prefix.",
    "DLMM LP cannot be paper-traded usefully — fee accrual, OOR fills, and close behavior all depend on real on-chain price action. Hence live tiny-wallet rather than DRY_RUN."
  ]
}
